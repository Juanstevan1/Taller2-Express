<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex - Pokenea</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container text-center mt-5">
        <div class="card mx-auto shadow-lg" style="max-width: 600px;">
            <div class="card-header bg-primary text-white">
                <h1 class="card-title display-4"><%= viewData.name %></h1>
            </div>
            <div class="card-body">
                <img src="<%= viewData.image %>" alt="<%= viewData.name %>" class="img-fluid mb-3" style="max-width: 100%;">
                <p class="card-text lead"><%= viewData.phrase %></p>
            </div>
            <div class="card-footer text-muted">
                Container ID: <%= viewData.containerId %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
